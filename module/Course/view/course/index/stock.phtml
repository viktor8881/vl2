<?php
    $this->mainMenu()->setActiveItemId('course');
?>

<?php
    echo '<div id="container" style="height: 400px; min-width: 310px"></div>';

    $this->headScript()->prependFile('https://code.jquery.com/jquery-3.1.1.min.js');
    $this->headScript()->prependFile('https://code.highcharts.com/stock/highstock.js');
    $this->headScript()->prependFile('https://code.highcharts.com/stock/modules/exporting.js');

    $this->headScript()->captureStart();
    echo "
        $.getJSON('" . $this->url('course-moex', ['id' => $this->currentExchange->getId()]) . "', function (data) {            
            Highcharts.stockChart('container', {
        
                rangeSelector: { selected: 1 },
                title: { text: 'MOEX акции ".$this->currentExchange->getName()."' },
        
                series: [{
                name: '".$this->currentExchange->getShortName()."',
                    data: data,
                    type: 'area',
                    threshold: null,
                    tooltip: {
                    valueDecimals: 2
                    },
                    fillColor: {
                    linearGradient: {
                        x1: 0,
                            y1: 0,
                            x2: 0,
                            y2: 1
                        },
                    stops: [
                        [0, Highcharts.getOptions().colors[0]],
                        [1, Highcharts.Color(Highcharts.getOptions().colors[0]).setOpacity(0).get('rgba')]
                    ]
                    }
                }]
            });
        });";
    $this->headScript()->captureEnd();

    foreach ($this->exchanges as $item) {
        if ($item['exchange']->getId() == $this->currentExchange->getId()) {
            ?>
            <div class="panel panel-default">
                <div class="panel-heading"><?= $this->currentExchange->getName() ?></div>
                <div class="panel-body row">
                    <div class="col-md-4">
                        <?php
                        foreach ($item['overtime'] as $analysis) {
                            echo '<div>' . $this->overtimeAnalysis($analysis) .  '</div>';
                        }
                        ?>
                    </div>
                    <div class="col-md-4">
                        <?php
                        foreach ($item['percent'] as $analysis) {
                            echo $this->percentAnalysis($analysis);
                        }
                        ?>
                    </div>
                    <div class="col-md-4">
                        <?php
                        foreach ($item['figure'] as $analysis) {
                            echo '<div>' . $this->figureAnalysis($analysis) .  '</div>';
                        }
                        ?>
                    </div>
                </div>
            </div>
            <?php
        }
    }


?>

<table class="table table-bordered">
    <thead>
    <tr>
        <th>Наименование</th>
        <th>Анализ</th>
        <th>Вес</th>
    </tr>
    </thead>
    <tbody>
    <?php if (count($this->exchanges)) : ?>
        <?php foreach ($this->exchanges as $item) : ?>
            <tr>
                <td>
                    <a href="<?= $this->url('course', ['action' => 'stock', 'id' => (int)$item['exchange']->getCode()]) ?>"><?= $this->escapeHtml($item['exchange']->getName())?></a>
                    <p><?= $item['dateTrade'] ?></p>
                </td>
                <td>
                    <div class="panel panel-default">
                        <div class="panel-heading">overtime (<?= count($item['overtime'])?>)</div>
                        <?php
                            if (count($item['overtime'])) {
                                echo '<div class="panel-body">';
                                foreach ($item['overtime'] as $analysis) {
                                    echo '<div>' . $this->overtimeAnalysis($analysis) .  '</div>';
                                }
                                echo '</div>';
                            }
                        ?>
                    </div>

                    <div class="panel panel-default">
                        <div class="panel-heading">percent (<?= count($item['percent'])?>)</div>
                        <?php
                            if (count($item['percent'])) {
                                echo '<div class="panel-body">';
                                foreach ($item['percent'] as $analysis) {
                                    echo $this->percentAnalysis($analysis);
                                }
                                echo '</div>';
                            }
                        ?>
                    </div>
                    <div class="panel panel-default">
                        <div class="panel-heading">figure (<?= count($item['figure'])?>)</div>
                        <?php
                            if (count($item['figure'])) {
                                echo '<div class="panel-body">';
                                foreach ($item['figure'] as $analysis) {
                                    echo '<div>' . $this->figureAnalysis($analysis) .  '</div>';
                                }
                                echo '</div>';
                            }
                        ?>
                    </div>

                </td>
                <td>&nbsp;
                   <?= ceil($item['weight']) ?>
                </td>
            </tr>
        <?php endforeach; ?>
    <?php else: ?>
        <tr><td colspan="2"><?= $this->escapeHtml('Акции не найдены') ?></td></tr>
    <?php endif; ?>
    </tbody>
</table>
